# Misc

Tools I keep around but rarely use.

## User-invoked

Reachable only when you type them (`disable-model-invocation: true`).

## Model-invoked

Model- or user-reachable (rich trigger phrasing so the model can reach for them).

- [fetch-zoom-transcript](fetch-zoom-transcript/SKILL.md) — Fetch a Zoom meeting transcript and write it to the scratchpad, routing around blank-doc and false-flag decoys.
